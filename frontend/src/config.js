// API Base URL Configuration
// Use VITE_API_URL environment variable if set (for production)
// Otherwise fallback to localhost (for development)
export const API_BASE_URL = import.meta.env.VITE_API_URL || 'https://weather-api-u4ln.onrender.com';

export default API_BASE_URL;
